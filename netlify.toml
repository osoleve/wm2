[build]
  publish = "dist"
  command = "npm run build"

[build.environment]
  # Netlify will automatically use environment variables you set in the dashboard
  # Updated: Force clean rebuild

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
