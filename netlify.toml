[build]
  publish = "dist"
  command = "npm run build"

[build.environment]
  # Netlify will automatically use environment variables you set in the dashboard
  # Just make sure your VITE_OPENROUTER_API_KEY is set in Netlify's environment variables

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
